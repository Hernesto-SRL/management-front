import {Box} from "@mui/material";
import Header from "../components/Header";

const Home = () => {
    return (
        <Box>
            <Header title="HOME PAGE" center/>
        </Box>
    );
};

export default Home;